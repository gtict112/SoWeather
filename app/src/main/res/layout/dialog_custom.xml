<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
             android:layout_width="fill_parent"
             android:layout_height="fill_parent"
             android:background="@drawable/shape_round_rect_white_border"
             android:clickable="true"
             android:orientation="vertical"
             android:padding="10dp">

    <LinearLayout style="@style/dialog_custom_title_layout">

        <TextView
            android:id="@+id/title"
            style="@style/dialog_custom_title"/>

        <LinearLayout
            android:id="@+id/content"
            style="@style/dialog_custom_message_layout">

            <TextView
                android:id="@+id/message"
                style="@style/dialog_custom_message"/>
        </LinearLayout>

        <View
            android:layout_width="fill_parent"
            android:layout_height="0.5dp"/>

        <LinearLayout style="@style/dialog_custom_button_layout">

            <Button
                android:id="@+id/negative_button"
                style="@style/dialog_custom_button.negative">
            </Button>
        </LinearLayout>
    </LinearLayout>

</FrameLayout>